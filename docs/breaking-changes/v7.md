# Breaking changes in v7

The library now requires semantic-release 23

## Upgrade instructions

Reinstall the peer dependencies in the project:

```shell
npx install-peerdeps --dev @open-turo/semantic-release-config
```
